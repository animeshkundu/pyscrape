# Core dependencies (also declared in pyproject.toml [project.dependencies])
requests>=2.31
lxml>=5.0
cssselect>=1.2
flask>=3.0

# Optional JS rendering — install with: pip install 'pyscrape[js]'
# playwright>=1.40

# Development
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
